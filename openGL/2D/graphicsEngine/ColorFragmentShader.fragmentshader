#version 330 core

// Ouput data
out vec3 color;
in vec3 fragColor;
void main(){

	// Output color = color specified in the vertex shader, 
	// interpolated between all 3 surrounding vertices
	color = fragColor;

}